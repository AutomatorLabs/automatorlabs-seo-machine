{
  "name": "automatorlabs-seo-machine",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "audit:seo": "node scripts/audit-seo.mjs",
    "verify": "npm run build && npm run audit:seo && npm run test:calculators && echo '\n[verify] Adding, removing, or renaming pages in this release? Resubmit the sitemap in Google Search Console before you consider it done — see docs/DEPLOYMENT.md.'",
    "preview": "astro preview",
    "test:calculators": "playwright test tests",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
