/**
 * Editable planning defaults only. These values do not determine current
 * contribution eligibility, catch-up eligibility, plan rules, or tax law.
 * Review the defaults annually and keep calculator inputs editable.
 */
export const fourOhOneKPlanningDefaults = {
  assumedAnnualEmployeeContributionLimit: 23500,
  assumedCatchUpContributionAmount: 7500,
  assumedCurrentTaxRatePercent: 24,
  assumedRetirementTaxRatePercent: 22,
};
