/**
 * Editable planning defaults only. These values are not a statement of
 * current eligibility, income phaseouts, contribution limits, or tax law.
 * Review the defaults annually and keep user inputs editable.
 */
export const rothIraPlanningDefaults = {
  assumedAnnualContributionLimit: 7000,
  assumedEarlyWithdrawalPenaltyPercent: 10,
  assumedTaxRatePercent: 22,
};
