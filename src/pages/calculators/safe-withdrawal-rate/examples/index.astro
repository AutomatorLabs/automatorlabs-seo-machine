---
import Layout from '../../../../layouts/Layout.astro';
import {
  EXPECTED_SAFE_WITHDRAWAL_RATE_SEO_PAGE_COUNT,
  safeWithdrawalRateSeoRecords,
} from '../../../../data/programmatic-seo/safe-withdrawal-rate';
import { auditSafeWithdrawalRateSeoRecords } from '../../../../lib/programmatic-seo/safe-withdrawal-rate';
import { createProgrammaticExampleGroups } from '../../../../lib/programmatic-seo/examples';
import {
  createBreadcrumbSchema,
  serializeJsonLd,
} from '../../../../lib/programmatic-seo/schema';

const audit = auditSafeWithdrawalRateSeoRecords(
  safeWithdrawalRateSeoRecords,
  EXPECTED_SAFE_WITHDRAWAL_RATE_SEO_PAGE_COUNT,
);
const site = Astro.site ?? new URL('https://automatorlabs.co');
const breadcrumbSchema = serializeJsonLd(
  createBreadcrumbSchema(
    [
      { name: 'Home', url: '/' },
      { name: 'Calculators', url: '/calculators/' },
      {
        name: 'Safe Withdrawal Rate Calculator',
        url: '/calculators/safe-withdrawal-rate-calculator/',
      },
      {
        name: 'Safe Withdrawal Rate Examples',
        url: '/calculators/safe-withdrawal-rate/examples/',
      },
    ],
    site,
  ),
);
const currency = new Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'USD',
  maximumFractionDigits: 0,
});
const groups = createProgrammaticExampleGroups(
  safeWithdrawalRateSeoRecords,
  [
    {
      id: 'lower-spending',
      title: 'Lower Spending Examples',
      description:
        'Compare $30,000 to $60,000 annual spending targets across portfolio sizes and withdrawal rates.',
      matches: (record) => record.annualSpending <= 60000,
    },
    {
      id: 'higher-spending',
      title: 'Higher Spending Examples',
      description:
        'Compare $75,000 to $150,000 annual spending targets for larger retirement portfolios.',
      matches: (record) => record.annualSpending > 60000,
    },
  ],
  (a, b) =>
    a.annualSpending - b.annualSpending ||
    a.portfolioValue - b.portfolioValue ||
    a.comparisonWithdrawalRatePercent -
      b.comparisonWithdrawalRatePercent ||
    a.years - b.years,
);
---

<Layout
  title="Safe Withdrawal Rate Examples | AutomatorLabs"
  description={`Browse ${audit.actualCount} safe withdrawal rate examples by portfolio value, annual spending, withdrawal rate, and retirement duration.`}
>
  <script type="application/ld+json" set:html={breadcrumbSchema} />

  <main id="main-content">
    <nav class="breadcrumbs" aria-label="Breadcrumb">
      <ol>
        <li><a href="/">Home</a></li>
        <li><a href="/calculators/">Calculators</a></li>
        <li>
          <a href="/calculators/safe-withdrawal-rate-calculator/">
            Safe Withdrawal Rate Calculator
          </a>
        </li>
        <li aria-current="page">Safe Withdrawal Rate Examples</li>
      </ol>
    </nav>

    <header class="page-header">
      <p class="eyebrow">Retirement planning examples</p>
      <h1>Safe Withdrawal Rate Examples</h1>
      <p class="intro">
        Browse {audit.actualCount} worked safe withdrawal rate examples covering
        annual spending needs, portfolio values, comparison withdrawal rates,
        and retirement durations.
      </p>
      <a class="primary-link" href="/calculators/safe-withdrawal-rate-calculator/">
        Calculate your safe withdrawal rate
      </a>
      <a class="secondary-link" href="/examples/">
        Browse all financial examples
      </a>
    </header>

    <section class="example-search" aria-label="Search safe withdrawal rate examples">
      <label for="safe-withdrawal-rate-example-search">
        Search safe withdrawal rate examples
      </label>
      <div class="search-row">
        <input
          id="safe-withdrawal-rate-example-search"
          type="search"
          placeholder="Try 40,000, 1,000,000, 4%, or 30 years"
          autocomplete="off"
          aria-controls="safe-withdrawal-rate-example-groups"
          aria-describedby="safe-withdrawal-rate-example-count"
        />
        <button id="clear-safe-withdrawal-rate-example-search" type="button" hidden>
          Clear search
        </button>
      </div>
      <p id="safe-withdrawal-rate-example-count" aria-live="polite">
        Showing {audit.actualCount} examples
      </p>
    </section>

    <div id="safe-withdrawal-rate-example-groups" class="example-groups">
      {groups.map((group) => (
        <section
          class="example-group"
          aria-labelledby={`${group.id}-heading`}
          data-safe-withdrawal-rate-example-group
        >
          <header>
            <div>
              <h2 id={`${group.id}-heading`}>{group.title}</h2>
              <p>{group.description}</p>
            </div>
            <span>{group.records.length} examples</span>
          </header>
          <ul>
            {group.records.map((record) => (
              <li
                data-safe-withdrawal-rate-example-card
                data-search-text={[
                  record.question,
                  record.slug,
                  record.portfolioValue,
                  record.portfolioValue.toLocaleString('en-US'),
                  record.annualSpending,
                  record.annualSpending.toLocaleString('en-US'),
                  record.comparisonWithdrawalRatePercent,
                  `${record.comparisonWithdrawalRatePercent}%`,
                  record.years,
                  `${record.years} years`,
                  'safe withdrawal rate',
                  'retirement',
                ]
                  .join(' ')
                  .toLocaleLowerCase()}
              >
                <a href={`/calculators/safe-withdrawal-rate/${record.slug}/`}>
                  <strong>{record.question}</strong>
                  <span>
                    {currency.format(record.annualSpending)} spending · {currency.format(record.portfolioValue)} portfolio · {record.comparisonWithdrawalRatePercent}% rate · {record.years} years
                  </span>
                </a>
              </li>
            ))}
          </ul>
        </section>
      ))}
    </div>

    <p id="no-safe-withdrawal-rate-examples" class="no-results" role="status" hidden>
      No safe withdrawal rate examples found. Try a portfolio, spending target, rate, or timeline.
    </p>
  </main>
</Layout>

<script>
  const input = document.querySelector<HTMLInputElement>(
    '#safe-withdrawal-rate-example-search',
  );
  const clearButton = document.querySelector<HTMLButtonElement>(
    '#clear-safe-withdrawal-rate-example-search',
  );
  const count = document.querySelector<HTMLElement>(
    '#safe-withdrawal-rate-example-count',
  );
  const noResults = document.querySelector<HTMLElement>(
    '#no-safe-withdrawal-rate-examples',
  );
  const cards = Array.from(
    document.querySelectorAll<HTMLElement>(
      '[data-safe-withdrawal-rate-example-card]',
    ),
  );
  const groups = Array.from(
    document.querySelectorAll<HTMLElement>(
      '[data-safe-withdrawal-rate-example-group]',
    ),
  );

  function updateExamples() {
    if (!input || !count || !noResults) return;

    const query = input.value.trim().toLocaleLowerCase();
    let visibleCount = 0;

    cards.forEach((card) => {
      const matches =
        query === '' || (card.dataset.searchText ?? '').includes(query);
      card.hidden = !matches;
      if (matches) visibleCount += 1;
    });

    groups.forEach((group) => {
      group.hidden = !group.querySelector(
        '[data-safe-withdrawal-rate-example-card]:not([hidden])',
      );
    });

    count.textContent = `Showing ${visibleCount} ${visibleCount === 1 ? 'example' : 'examples'}`;
    noResults.hidden = visibleCount !== 0;
    if (clearButton) clearButton.hidden = query === '';
  }

  input?.addEventListener('input', updateExamples);
  clearButton?.addEventListener('click', () => {
    if (!input) return;
    input.value = '';
    updateExamples();
    input.focus();
  });
</script>

<style>
  main {
    width: min(1120px, calc(100% - 2rem));
    margin: 0 auto;
    padding: 4.5rem 0;
  }

  .breadcrumbs {
    margin-bottom: 1.5rem;
    color: var(--muted-text-color);
    font-size: 0.9rem;
  }

  .breadcrumbs ol {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    align-items: center;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .breadcrumbs li:not(:last-child)::after {
    content: '›';
    margin-left: 0.45rem;
    color: var(--border-color);
  }

  .page-header {
    max-width: 780px;
    margin-bottom: 2rem;
  }

  .eyebrow {
    margin: 0 0 0.75rem;
    color: var(--accent-color);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    font-size: clamp(2.4rem, 6vw, 4.6rem);
    line-height: 1;
    letter-spacing: -0.06em;
  }

  .intro {
    color: var(--muted-text-color);
    font-size: 1.1rem;
    line-height: 1.7;
  }

  .primary-link,
  .secondary-link {
    display: inline-flex;
    margin: 0.5rem 0.75rem 0 0;
    border-radius: 999px;
    padding: 0.85rem 1.1rem;
    font-weight: 700;
    text-decoration: none;
  }

  .primary-link {
    background: var(--accent-color);
    color: var(--accent-text-color);
  }

  .secondary-link {
    border: 1px solid var(--border-color);
    color: var(--text-color);
  }

  .example-search,
  .example-group {
    border: 1px solid var(--border-color);
    border-radius: 1.25rem;
    background: var(--card-bg);
    box-shadow: var(--card-shadow);
  }

  .example-search {
    margin-bottom: 1.5rem;
    padding: 1rem;
  }

  .example-search label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 700;
  }

  .search-row {
    display: flex;
    gap: 0.75rem;
  }

  input {
    width: 100%;
    border: 1px solid var(--border-color);
    border-radius: 999px;
    padding: 0.8rem 1rem;
    background: var(--input-bg);
    color: var(--text-color);
  }

  button {
    border: 0;
    border-radius: 999px;
    padding: 0.8rem 1rem;
    background: var(--text-color);
    color: var(--background-color);
    font-weight: 700;
  }

  .example-groups {
    display: grid;
    gap: 1.25rem;
  }

  .example-group {
    padding: 1rem;
  }

  .example-group header {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    align-items: flex-start;
    margin-bottom: 1rem;
  }

  .example-group h2,
  .example-group p {
    margin: 0;
  }

  .example-group p,
  .example-group span,
  .no-results {
    color: var(--muted-text-color);
  }

  .example-group ul {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 0.75rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .example-group a {
    display: grid;
    gap: 0.35rem;
    height: 100%;
    border: 1px solid var(--border-color);
    border-radius: 1rem;
    padding: 0.9rem;
    color: var(--text-color);
    text-decoration: none;
  }

  .example-group a span {
    color: var(--muted-text-color);
    font-size: 0.92rem;
    line-height: 1.5;
  }

  .no-results {
    padding: 1rem;
  }

  @media (max-width: 640px) {
    .search-row,
    .example-group header {
      flex-direction: column;
    }

    button {
      width: 100%;
    }
  }
</style>
